<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="using:PhotoStudio.Desktop.ViewModels"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d" d:DesignWidth="1000" d:DesignHeight="700"
        x:Class="PhotoStudio.Desktop.Views.MainWindow"
        x:DataType="vm:MainWindowViewModel"
        Title="Система управления фотостудией"
        TransparencyLevelHint="AcrylicBlur"
        Background="White"
        WindowStartupLocation="CenterScreen"
        Width="1100" Height="750"
        MinWidth="900" MinHeight="600"
        Icon="/Assets/avalonia-logo.ico">

    <Grid RowDefinitions="Auto,*">
        <!-- Заголовок -->
        <Border Grid.Row="0" Background="#3498db" Padding="20,15" BoxShadow="0 2 10 0 #40000000">
            <Grid ColumnDefinitions="Auto,*,Auto">
                <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="15" VerticalAlignment="Center">
                    <TextBlock Text="📸" FontSize="28"/>
                    <StackPanel>
                        <TextBlock Text="Система управления фотостудией" 
                                FontSize="24" FontWeight="Bold" Foreground="White"/>
                        <TextBlock Text="Управление клиентами, фотосессиями и фотографиями" 
                                FontSize="14" Foreground="White" Opacity="0.8" Margin="0,5,0,0"/>
                    </StackPanel>
                </StackPanel>
                
                <!-- Правая часть заголовка -->
                <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="15" VerticalAlignment="Center">
                    <Border Background="#ffffff30" CornerRadius="4" Padding="10,8">
                        <StackPanel Orientation="Horizontal" Spacing="10">
                            <TextBlock Text="👤" FontSize="16" VerticalAlignment="Center"/>
                            <StackPanel>
                                <TextBlock Text="Администратор" Foreground="White" FontWeight="SemiBold"/>
                                <TextBlock Text="admin@photostudio.ru" Foreground="White" Opacity="0.8" FontSize="12"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                    <Button Content="⚙️ Настройки" Background="#ffffff30"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Основной контент -->
        <Grid Grid.Row="1">
            <TabControl Margin="0,10,0,0" SelectedIndex="{Binding SelectedTabIndex}">
                <!-- Вкладка Клиенты -->
                <TabItem Header="Клиенты">
                    <Grid RowDefinitions="Auto,*">
                        <!-- Панель действий -->
                        <Border Grid.Row="0" Background="#f8f9fa" Padding="15" CornerRadius="6" Margin="10,10,10,15"
                                BorderBrush="#e9ecef" BorderThickness="1" BoxShadow="0 2 5 0 #20000000">
                            <Grid ColumnDefinitions="*,Auto">
                                <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="10">
                                    <Button Content="➕ Добавить клиента" Command="{Binding AddClientCommand}"/>
                                    <Button Content="🔄 Обновить" Command="{Binding LoadDataCommand}"
                                            Background="#28a745"/>
                                </StackPanel>
                                <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="10">
                                    <TextBox Width="250" Watermark="Поиск клиентов..." Padding="10,7">
                                        <TextBox.InnerLeftContent>
                                            <TextBlock Text="🔍" Margin="8,0,0,0" VerticalAlignment="Center" FontSize="14"/>
                                        </TextBox.InnerLeftContent>
                                    </TextBox>
                                    <Button Content="Поиск" Background="#6c757d"/>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <!-- Таблица клиентов -->
                        <Border Grid.Row="1" Margin="10,0,10,10" BorderBrush="#e9ecef" BorderThickness="1" 
                                CornerRadius="6" Padding="0" ClipToBounds="True" BoxShadow="0 2 8 0 #20000000">
                            <DataGrid ItemsSource="{Binding Clients}" AutoGenerateColumns="False" IsReadOnly="True">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="70"/>
                                    <DataGridTextColumn Header="Имя" Binding="{Binding Name}" Width="*"/>
                                    <DataGridTextColumn Header="Телефон" Binding="{Binding Phone}" Width="160"/>
                                    <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="200"/>
                                    <DataGridTemplateColumn Header="Действия" Width="180">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <StackPanel Orientation="Horizontal" Spacing="5" HorizontalAlignment="Center">
                                                    <Button Content="✎" ToolTip.Tip="Редактировать" Background="#ffc107" 
                                                            Foreground="#212529" Padding="8,5" CornerRadius="4"/>
                                                    <Button Content="👁" ToolTip.Tip="Просмотр сессий" Background="#17a2b8" 
                                                            Padding="8,5" CornerRadius="4"/>
                                                    <Button Content="✗" ToolTip.Tip="Удалить" Background="#dc3545" 
                                                            Padding="8,5" CornerRadius="4"/>
                                                </StackPanel>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                </DataGrid.Columns>
                            </DataGrid>
                        </Border>
                    </Grid>
                </TabItem>

                <!-- Вкладка Фотосессии -->
                <TabItem Header="Фотосессии">
                    <Grid RowDefinitions="Auto,*">
                        <!-- Панель действий -->
                        <Border Grid.Row="0" Background="#f8f9fa" Padding="15" CornerRadius="6" Margin="10,10,10,15"
                                BorderBrush="#e9ecef" BorderThickness="1" BoxShadow="0 2 5 0 #20000000">
                            <Grid ColumnDefinitions="*,Auto">
                                <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="10">
                                    <Button Content="➕ Добавить сессию" Command="{Binding AddSessionCommand}"/>
                                    <Button Content="🔄 Обновить" Command="{Binding LoadDataCommand}"
                                            Background="#28a745"/>
                                </StackPanel>
                                <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="10">
                                    <ComboBox Width="150" SelectedIndex="0" PlaceholderText="Статус">
                                        <ComboBoxItem>Все статусы</ComboBoxItem>
                                        <ComboBoxItem>Запланированные</ComboBoxItem>
                                        <ComboBoxItem>Завершенные</ComboBoxItem>
                                        <ComboBoxItem>Отмененные</ComboBoxItem>
                                    </ComboBox>
                                    <TextBox Width="200" Watermark="Поиск..." Padding="10,7">
                                        <TextBox.InnerLeftContent>
                                            <TextBlock Text="🔍" Margin="8,0,0,0" VerticalAlignment="Center" FontSize="14"/>
                                        </TextBox.InnerLeftContent>
                                    </TextBox>
                                    <Button Content="Поиск" Background="#6c757d"/>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <!-- Таблица фотосессий -->
                        <Border Grid.Row="1" Margin="10,0,10,10" BorderBrush="#e9ecef" BorderThickness="1" 
                                CornerRadius="6" Padding="0" ClipToBounds="True" BoxShadow="0 2 8 0 #20000000">
                            <DataGrid ItemsSource="{Binding Sessions}" AutoGenerateColumns="False" IsReadOnly="True">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="70"/>
                                    <DataGridTextColumn Header="Дата" Binding="{Binding Date}" Width="180"/>
                                    <DataGridTextColumn Header="Длит. (мин)" Binding="{Binding Duration}" Width="110"/>
                                    <DataGridTextColumn Header="Стоимость" Binding="{Binding Price}" Width="120"/>
                                    <DataGridTemplateColumn Header="Статус" Width="140">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <Border CornerRadius="4" Padding="5,3" Margin="5,2"
                                                        Background="{Binding Status, Converter={StaticResource StatusColorConverter}}">
                                                    <TextBlock Text="{Binding Status}" Foreground="White" 
                                                               HorizontalAlignment="Center" FontWeight="SemiBold"/>
                                                </Border>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                    <DataGridTextColumn Header="Клиент ID" Binding="{Binding ClientId}" Width="100"/>
                                    <DataGridTemplateColumn Header="Действия" Width="180">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <StackPanel Orientation="Horizontal" Spacing="5" HorizontalAlignment="Center">
                                                    <Button Content="✎" ToolTip.Tip="Редактировать" Background="#ffc107" 
                                                            Foreground="#212529" Padding="8,5" CornerRadius="4"/>
                                                    <Button Content="🖼" ToolTip.Tip="Фотографии" Background="#17a2b8" 
                                                            Padding="8,5" CornerRadius="4"/>
                                                    <Button Content="✗" ToolTip.Tip="Удалить" Background="#dc3545" 
                                                            Padding="8,5" CornerRadius="4"/>
                                                </StackPanel>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                </DataGrid.Columns>
                            </DataGrid>
                        </Border>
                    </Grid>
                </TabItem>

                <!-- Вкладка Фотографии -->
                <TabItem Header="Фотографии">
                    <Grid RowDefinitions="Auto,*">
                        <!-- Панель действий -->
                        <Border Grid.Row="0" Background="#f8f9fa" Padding="15" CornerRadius="6" Margin="10,10,10,15"
                                BorderBrush="#e9ecef" BorderThickness="1" BoxShadow="0 2 5 0 #20000000">
                            <Grid ColumnDefinitions="*,Auto">
                                <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="10">
                                    <Button Content="➕ Добавить фото" Command="{Binding AddPhotoCommand}"/>
                                    <Button Content="🔄 Обновить" Command="{Binding LoadDataCommand}"
                                            Background="#28a745"/>
                                </StackPanel>
                                <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="10">
                                    <TextBox Width="200" Watermark="Поиск по имени файла..." Padding="10,7">
                                        <TextBox.InnerLeftContent>
                                            <TextBlock Text="🔍" Margin="8,0,0,0" VerticalAlignment="Center" FontSize="14"/>
                                        </TextBox.InnerLeftContent>
                                    </TextBox>
                                    <ComboBox Width="150" SelectedIndex="0" PlaceholderText="Сессия">
                                        <ComboBoxItem>Все сессии</ComboBoxItem>
                                    </ComboBox>
                                    <Button Content="Поиск" Background="#6c757d"/>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <!-- Таблица фотографий -->
                        <Border Grid.Row="1" Margin="10,0,10,10" BorderBrush="#e9ecef" BorderThickness="1" 
                                CornerRadius="6" Padding="0" ClipToBounds="True" BoxShadow="0 2 8 0 #20000000">
                            <DataGrid ItemsSource="{Binding Photos}" AutoGenerateColumns="False" IsReadOnly="True">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="70"/>
                                    <DataGridTextColumn Header="Имя файла" Binding="{Binding Filename}" Width="*"/>
                                    <DataGridTextColumn Header="Путь" Binding="{Binding Path}" Width="200"/>
                                    <DataGridTextColumn Header="Сессия ID" Binding="{Binding SessionId}" Width="100"/>
                                    <DataGridTextColumn Header="Создано" Binding="{Binding CreatedAt}" Width="180"/>
                                    <DataGridTemplateColumn Header="Действия" Width="120">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <StackPanel Orientation="Horizontal" Spacing="5" HorizontalAlignment="Center">
                                                    <Button Content="👁" ToolTip.Tip="Просмотр" Background="#17a2b8" 
                                                            Padding="8,5" CornerRadius="4"/>
                                                    <Button Content="✗" ToolTip.Tip="Удалить" Background="#dc3545" 
                                                            Padding="8,5" CornerRadius="4"/>
                                                </StackPanel>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                </DataGrid.Columns>
                            </DataGrid>
                        </Border>
                    </Grid>
                </TabItem>

                <!-- Вкладка Аналитика и Отчеты -->
                <TabItem Header="Аналитика">
                    <Grid RowDefinitions="Auto,*">
                        <Border Grid.Row="0" Background="#f8f9fa" Padding="15" CornerRadius="4" Margin="10,10,10,15"
                                BorderBrush="#e9ecef" BorderThickness="1">
                            <StackPanel>
                                <TextBlock Text="Статистика фотостудии" Classes="heading"/>
                                <TextBlock Text="Основные показатели работы" Classes="subheading"/>
                            </StackPanel>
                        </Border>
                        
                        <Grid Grid.Row="1" ColumnDefinitions="*,*,*" RowDefinitions="*,*" Margin="5">
                            <!-- Информационные карточки -->
                            <Border Grid.Column="0" Grid.Row="0" Classes="card">
                                <StackPanel>
                                    <TextBlock Text="Всего клиентов" FontSize="16" Opacity="0.7"/>
                                    <TextBlock Text="25" FontSize="36" FontWeight="Bold" Foreground="#3498db"/>
                                    <TextBlock Text="↑ 5 за последний месяц" Foreground="#28a745" Margin="0,5,0,0"/>
                                </StackPanel>
                            </Border>
                            
                            <Border Grid.Column="1" Grid.Row="0" Classes="card">
                                <StackPanel>
                                    <TextBlock Text="Завершенных сессий" FontSize="16" Opacity="0.7"/>
                                    <TextBlock Text="42" FontSize="36" FontWeight="Bold" Foreground="#3498db"/>
                                    <TextBlock Text="↑ 12 за последний месяц" Foreground="#28a745" Margin="0,5,0,0"/>
                                </StackPanel>
                            </Border>
                            
                            <Border Grid.Column="2" Grid.Row="0" Classes="card">
                                <StackPanel>
                                    <TextBlock Text="Общий доход" FontSize="16" Opacity="0.7"/>
                                    <TextBlock Text="₽ 126,000" FontSize="36" FontWeight="Bold" Foreground="#3498db"/>
                                    <TextBlock Text="↑ ₽ 45,000 за последний месяц" Foreground="#28a745" Margin="0,5,0,0"/>
                                </StackPanel>
                            </Border>
                            
                            <Border Grid.Column="0" Grid.Row="1" Classes="card" Grid.ColumnSpan="2">
                                <StackPanel>
                                    <TextBlock Text="Популярные типы съемок" Classes="heading"/>
                                    <Grid ColumnDefinitions="*,Auto" RowDefinitions="Auto,Auto,Auto,Auto" Margin="0,15,0,0">
                                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Портретная съемка"/>
                                        <TextBlock Grid.Row="0" Grid.Column="1" Text="45%" FontWeight="SemiBold"/>
                                        
                                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Семейная съемка" Margin="0,10,0,0"/>
                                        <TextBlock Grid.Row="1" Grid.Column="1" Text="25%" FontWeight="SemiBold" Margin="0,10,0,0"/>
                                        
                                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Предметная съемка" Margin="0,10,0,0"/>
                                        <TextBlock Grid.Row="2" Grid.Column="1" Text="15%" FontWeight="SemiBold" Margin="0,10,0,0"/>
                                        
                                        <TextBlock Grid.Row="3" Grid.Column="0" Text="Другое" Margin="0,10,0,0"/>
                                        <TextBlock Grid.Row="3" Grid.Column="1" Text="15%" FontWeight="SemiBold" Margin="0,10,0,0"/>
                                    </Grid>
                                </StackPanel>
                            </Border>
                            
                            <Border Grid.Column="2" Grid.Row="1" Classes="card">
                                <StackPanel>
                                    <TextBlock Text="Ближайшие сессии" Classes="heading"/>
                                    <StackPanel Margin="0,15,0,0" Spacing="12">
                                        <Border Background="#e3f2fd" CornerRadius="4" Padding="10">
                                            <StackPanel>
                                                <TextBlock Text="Завтра, 10:00" FontWeight="Bold"/>
                                                <TextBlock Text="Анна Смирнова, портретная съемка" Margin="0,3,0,0"/>
                                            </StackPanel>
                                        </Border>
                                        <Border Background="#e3f2fd" CornerRadius="4" Padding="10">
                                            <StackPanel>
                                                <TextBlock Text="Завтра, 14:00" FontWeight="Bold"/>
                                                <TextBlock Text="Семья Ивановых, семейная съемка" Margin="0,3,0,0"/>
                                            </StackPanel>
                                        </Border>
                                    </StackPanel>
                                </StackPanel>
                            </Border>
                        </Grid>
                    </Grid>
                </TabItem>
            </TabControl>
        </Grid>
    </Grid>
</Window> 